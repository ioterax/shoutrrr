; Keep PHP's transport limits aligned with Laravel's image validation.
; The image editor sends the composed image and its source in one multipart
; request, so post_max_size needs headroom above two 8 MiB files.
memory_limit = 512M
upload_max_filesize = 8M
post_max_size = 20M
max_file_uploads = 20
